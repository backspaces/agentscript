import View from '../src/TwoDraw.js'

const viewOpts = {
    patchSize: 15,
    useSprites: true
}

const worldOpts = {
    minX: -16,
    maxX: 16,
    minY: -16,
    maxY: 16
}

export { View, viewOpts, worldOpts }